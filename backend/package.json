{
  "name": "remove-bg-backend",
  "version": "1.0.1",
  "description": "A backend server to remove background from images using TensorFlow.js and Express.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "postinstall": "npm audit fix --force"
  },
  "dependencies": {
    "@tensorflow/tfjs-node": "^4.0.0",
    "canvas": "^2.8.0",
    "express": "^4.18.2",
    "multer": "^1.4.4-lts.1"
  }
}
